## 2024-05-22 - Dynamic Form Accessibility
**Learning:** When using vanilla JS templates for dynamic forms, explicit IDs and label associations are often missed.
**Action:** When cloning template content, always generate unique IDs (e.g., using index) and explicitly link labels to inputs using `for` and `id` attributes.
